$(document).ready(function () {
    // paralax effect
    $('.parallax').parallax();

    // materialboxed effect
    $('.materialboxed').materialbox();

    // sidenav effect
    $('.sidenav').sidenav();

    // slider effect
    $('.slider').slider();

    // modal effect    
    $('.modal').modal();

    // collapsible effect
    $('.collapsible').collapsible();
});